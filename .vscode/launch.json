{
    "configurations": [
        {
            "name": "Launch Program",
            "program": "${workspaceFolder}/brace-style/test.js",
            "request": "launch",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "pwa-node"
        }

    ]
}